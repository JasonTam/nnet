function [ NN_trained ] = backPropLearn( init, train,...
    n_epochs, alpha )
%BACKPROPLEARN Summary of this function goes here
%   Back propagation learning
%   init = initial struct
%   train = train struct
%   alpha = learning rate

L = numel(NN_init)+1;   % Should be 3 for 1 hidden layer


% Initialize weights
% (Skipping this because NN is already initialized)

for epoch = 1:n_epochs
   for e = 1:train.N(1)
       
       
   end
    
end



end

